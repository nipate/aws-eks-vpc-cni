apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: block-frontend-egress
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: frontend
  policyTypes:
  - Egress
  egress: []